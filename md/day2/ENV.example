# Toolgate — ENV example (per service)

NODE_ENV=production
SENTRY_DSN=
HMAC_KEY=replace_me_with_long_random_secret
ALLOW_HOSTS=internal.local,httpbin.org

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB
REDIS_URL=redis://:PASSWORD@HOST:PORT/0

TOOLGATE_COLLECTOR_URL=http://localhost:8785/v1/events
TOOLGATE_PROXY_URL=http://localhost:8787/v1/proxy
TOOLGATE_SANITIZER_URL=http://localhost:8786/v1/sanitize-context
