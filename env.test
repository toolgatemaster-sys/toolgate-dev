# Test Environment Variables
# Firma del cliente hacia el Gateway (si aplica)
TOOLGATE_CLIENT_HMAC_KEY=dev-client-key

# Firma interna del Gateway hacia el Collector
TOOLGATE_GATEWAY_HMAC_KEY=dev-internal-key

# URLs de los servicios en Railway
GATEWAY_URL=https://toolgate-gateway.up.railway.app
COLLECTOR_URL=https://toolgate-collector.up.railway.app
SANITIZER_URL=https://toolgate-sanitizer.up.railway.app
WEB_URL=https://toolgate-web.up.railway.app

# Configuración de allowlist para tests
ALLOW_HOSTS=httpbin.org,api.example.com,example.com
